using UnityEngine;

public class Defeat : MonoBehaviour
{
    // Quand un joueur n'a plus de structures, il perd la partie
    
    // SI un joueur déco alors ses structures deviennent neutre et on destroy toutes ses unités
    
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
