using UnityEngine;

public class StructureAttribution : MonoBehaviour
{
    // Attribution des camps au début de parties 
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
